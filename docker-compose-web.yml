# How to run:

version: '2'
services:
  paisagemweb:
    container_name: paisagemweb
    build:
      context: .
      dockerfile: Dockerfile_web
    image: paisagem-web:latest
    ports:
      - "4500:80"
      - "443:443"
    networks:
      - paisagem-network

networks:
    paisagem-network: